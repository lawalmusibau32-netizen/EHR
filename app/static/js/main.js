document.addEventListener("DOMContentLoaded", () => {
  const offcanvas = document.querySelector(".offcanvas");
  if (offcanvas && window.bootstrap) {
    // Bootstrap handles the sidebar toggle on mobile.
  }
});
